{
  "name": "church_app_api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server",
    "test": " SET \"NODE_ENV=test\" && nodemon --exec \"mocha --recursive  \"",
    "test2": " SET \"NODE_ENV=test\" && nodemon --exec \"mocha --recursive -R min \""
    
  },
  "author": "Funminiyi Adekunle",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "mocha": "^3.2.0",
    "mongoose": "^4.6.8"
  },
  "devDependencies": {
    "expect": "^1.20.2",
    "supertest": "^2.0.1"
  }
}
